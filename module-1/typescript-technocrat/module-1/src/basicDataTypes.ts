{
  //basic data types

  // string

  let firstName: string = "nahid";
  // number
  let roll: number = 562;
  // boolean
  let isAdmin: boolean = true;
  //undefined
  let lastName: undefined = undefined;
  // null

  let y: null = null;
  // array
  let friends: string[] = ["Bipu", "Tausif"];

  let arrOfNumber: number[] = [5, 6];

  // tuple

  let ageName: [number, string] = [50, "Md. Nahid"];

  // ageName[0] = "90"; // Type 'string' is not assignable to type 'number'.ts(2322)
}
