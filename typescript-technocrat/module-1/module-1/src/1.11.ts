{
  const age: number = 18;

  //   console.log(age > 18 ? "Adult" : "Child");

  // nullish coalescing operator

  const issAuthenticate = undefined
  ;

  const res1 = issAuthenticate ?? "guest";
  console.log({ res1 });
}
